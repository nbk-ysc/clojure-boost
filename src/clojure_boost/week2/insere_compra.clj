(ns clojure-boost.week2.insere-compra)

;(defn inserir-compra [nova-compra compras]
;  (let [id-gerado (cria-id compras)
;        compra-com-id (assoc nova-compra :id id-gerado)]
;    (conj compras compra-com-id)))
